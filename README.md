# git-basic

```
// 여러 파일 스테이징
git add 파일1 파일2

// 모든 파일 스테이징
git add .

// 상태창
git status

// 스테이징 취소
git restore --staged 파일1
git restore --staged .

// 커밋
git commit -m '메세지'

// 로그 확인
git log [--all] [--online] [--graph]

```
